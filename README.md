# tea-cli-skill

GitHub-hosted Codex skill repo for working with Gitea through the `tea` CLI.

This repository exposes one skill:

- `skills/tea-cli`

The layout is intentionally minimal so GitHub-based skill installers can consume it directly.
